/*var mongoose = require('mongoose');

var schritteSchema = mongoose.Schema({
    idGantt: String,
    resource: String,
    startDatum: Date,
    endDatum: Date,
    dauer: Number,
    statusInProzent: Number,
    abhaengigkeitidGantt: String,
    reihenfolge: Number,
});

var Schritte = mongoose.model('Schritte', schritteSchema);

module.exports = Schritte;
    */